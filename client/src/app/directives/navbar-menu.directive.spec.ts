import { NavbarMenuDirective } from './navbar-menu.directive';

describe('NavbarMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarMenuDirective();
    expect(directive).toBeTruthy();
  });
});
